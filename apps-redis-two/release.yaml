# temporal to delete
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: redis
  namespace: redis-two
  labels:
    env: two
    release: prometheus
spec:
  interval: 10m
  chartRef:
    kind: OCIRepository
    name: bitnami
    namespace: redis-two
  values: